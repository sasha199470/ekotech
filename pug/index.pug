extend layout.pug

include mixins/two-parts-block

block content
  include includes/name
  +two-parts-block(about, "./images/dummy.svg")
  include includes/projects
  include includes/service
  +two-parts-block(resources, "./images/dummy.svg", true)