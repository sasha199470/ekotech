div.container-header
  header
    div.navbar-toggler-wrapper
      div#navbar-toggler
        span
        span
        span
    nav
      each item in navbar
        a.as-text(href="#"+item.id)= item.titleRu
    div.logo
      img(src=site.logo)
      span ЭКОТЕХ