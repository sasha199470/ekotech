extends ./abstract-project

block image
  img.project-image(src=projects[0].image)
block content
  div.container
    +getProjectContent(0)
    +modalWindow(0, 'Hello', 'images/projects/project-img-0.jpg')