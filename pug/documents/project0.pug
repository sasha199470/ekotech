extends ./abstract-project

block image
  img.project-image(src=projects[0].image)
block content
  div.container
    +getProjectContent(0)
    ul.main-color
      each city, index in projects[0].cities
        +modalWindow(index, city.name, city.images.dir, city.images.count)