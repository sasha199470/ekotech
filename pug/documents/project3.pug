extends ./abstract-project

block image
  idiv.project-image(style={'background-image': `url(${projects[3].image})`})
block content
  div.container
    +getProjectContent(3)
    div.main-color
      +modalWindow(3, "Посмотреть фотографии", projects[3].images.dir, projects[3].images.count)