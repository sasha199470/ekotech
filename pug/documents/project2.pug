extends ./abstract-project

block image
  img.project-image(src=projects[2].image)
block content
  div.container
    +getProjectContent(2)
    div.main-color
      +modalWindow(2, "Посмотреть фотографии", projects[2].images.dir, projects[2].images.count)